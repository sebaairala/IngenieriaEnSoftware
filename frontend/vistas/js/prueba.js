/*console.log("try");
var a = {
	 A:0,
	 B:0,
	 C:0,
	 D:0,
	 resultado:function(){

	 	if(a.C > a.B &&
	 	   a.D > a.B &&
	 	   a.D > a.C &&
	 	   a.D < a.A){

	 		return true;
	 	}

	 	return false;
	 },
	 intervalo:setInterval(function(){

	 	a.A = Math.ceil(Math.random()*4);
	 	a.B = Math.ceil(Math.random()*4);
	 	a.C = Math.ceil(Math.random()*4);
	 	a.D = Math.ceil(Math.random()*4);

	 	if(a.resultado()){
	 		clearInterval(a.intervalo);
	 		console.log("atleta: ", a.A);
	 		console.log("atleta: ", a.B);
	 		console.log("atleta: ", a.C);
	 		console.log("atleta: ", a.D);
	 	}

	 } , 10)
}
*/

var b = {
	Mac: {edad:0,velocidad:0,tono:2},
	Smith: {edad:0,velocidad:0,tono:2},
	Jack: {edad:0,velocidad:0,tono:2},
	Willy: {edad:0,velocidad:0,tono:2},
	resultado: function(){
		if()
	}
}